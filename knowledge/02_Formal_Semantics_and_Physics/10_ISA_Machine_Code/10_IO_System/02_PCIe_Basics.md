# PCIe基础详解

## 目录

- [PCIe基础详解](#pcie基础详解)
  - [目录](#目录)
  - [1. 概述](#1-概述)
    - [1.1 PCIe在系统中的位置](#11-pcie在系统中的位置)
    - [1.2 PCIe vs 传统PCI](#12-pcie-vs-传统pci)
  - [2. PCIe架构概述](#2-pcie架构概述)
    - [2.1 物理层拓扑](#21-物理层拓扑)
    - [2.2 配置空间](#22-配置空间)
  - [3. 分层结构](#3-分层结构)
    - [3.1 三层模型](#31-三层模型)
    - [3.2 LTSSM状态机](#32-ltssm状态机)
  - [4. TLP包格式](#4-tlp包格式)
    - [4.1 TLP通用格式](#41-tlp通用格式)
    - [4.2 具体TLP类型](#42-具体tlp类型)
    - [4.3 事务排序规则](#43-事务排序规则)
  - [5. 枚举与配置](#5-枚举与配置)
    - [5.1 枚举过程](#51-枚举过程)
    - [5.2 Linux枚举实现](#52-linux枚举实现)
  - [6. DMA与PCIe](#6-dma与pcie)
    - [6.1 PCIe DMA机制](#61-pcie-dma机制)
    - [6.2 Linux PCIe DMA编程](#62-linux-pcie-dma编程)
  - [7. 中断机制](#7-中断机制)
    - [7.1 PCIe中断类型](#71-pcie中断类型)
    - [7.2 Linux MSI/MSI-X编程](#72-linux-msimsi-x编程)
  - [8. 电源管理](#8-电源管理)
    - [8.1 PCIe电源状态](#81-pcie电源状态)
    - [8.2 Linux电源管理](#82-linux电源管理)
  - [9. Linux PCIe驱动](#9-linux-pcie驱动)
    - [9.1 完整PCIe驱动框架](#91-完整pcie驱动框架)
  - [10. Verilog实现](#10-verilog实现)
    - [10.1 简化PCIe Endpoint](#101-简化pcie-endpoint)
  - [总结](#总结)

---

## 1. 概述

### 1.1 PCIe在系统中的位置

PCI Express (PCIe) 是现代计算机系统中使用最广泛的高速串行总线标准，用于连接CPU、内存和外设。

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                    系统PCIe架构全景                                          │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│   ┌─────────────────────────────────────────────────────────────────────┐  │
│   │                         CPU Complex                                │  │
│   │  ┌─────────────┐         ┌─────────────┐         ┌─────────────┐   │  │
│   │  │   Core 0    │         │   Core 1    │         │   Core N    │   │  │
│   │  │ ┌─────────┐ │         │ ┌─────────┐ │         │ ┌─────────┐ │   │  │
│   │  │ │  L1/L2  │ │         │ │  L1/L2  │ │         │ │  L1/L2  │ │   │  │
│   │  │ │  Cache  │ │         │ │  Cache  │ │         │ │  Cache  │ │   │  │
│   │  │ └────┬────┘ │         │ └────┬────┘ │         │ └────┬────┘ │   │  │
│   │  └──────┼──────┘         └──────┼──────┘         └──────┼──────┘   │  │
│   │         │                       │                       │           │  │
│   │         └───────────────────────┼───────────────────────┘           │  │
│   │                                 │                                   │  │
│   │  ═══════════════════════════════╪══════════════════════════════     │  │
│   │            Ring/ Mesh Interconnect (片上互联)                       │  │
│   │  ═══════════════════════════════╪══════════════════════════════     │  │
│   │                                 │                                   │  │
│   │                         ┌───────┴───────┐                           │  │
│   │                         │     LLC       │                           │  │
│   │                         │  (Last Level  │                           │  │
│   │                         │    Cache)     │                           │  │
│   │                         └───────┬───────┘                           │  │
│   │                                 │                                   │  │
│   └─────────────────────────────────┼───────────────────────────────────┘  │
│                                     │                                       │
│   ══════════════════════════════════╪══════════════════════════════════     │
│                System Agent / Memory Controller                             │
│   ══════════════════════════════════╪══════════════════════════════════     │
│                                     │                                       │
│   ┌─────────────────────────────────┼───────────────────────────────────┐  │
│   │                         PCIe Root Complex                         │  │
│   │                     ┌─────────────┴─────────────┐                 │  │
│   │                     │                             │                 │  │
│   │   ══════════════════╪═════════════════════════════╪══════════════ │  │
│   │              PCIe 0 x16 (Graphics/AI)             │                 │  │
│   │   ══════════════════╪═════════════════════════════╪══════════════ │  │
│   │                     │                             │                 │  │
│   │         ┌───────────┴───────────┐                 │                 │  │
│   │         │     GPU / AI Accel    │                 │                 │  │
│   │         │   (H100, RTX 4090)    │                 │                 │  │
│   │         └───────────────────────┘                 │                 │  │
│   │                                                   │                 │  │
│   │   ════════════════════════════════════════════════╪══════════════ │  │
│   │              PCIe Switch / PCH                     │                 │  │
│   │   ════════════════════════════════════════════════╪══════════════ │  │
│   │                                                   │                 │  │
│   │       ┌─────────────┬─────────────┬─────────────┐ │                 │  │
│   │       ▼             ▼             ▼             ▼ │                 │  │
│   │   ┌───────┐    ┌───────┐    ┌───────┐    ┌───────┐│                 │  │
│   │   │NVMe   │    │NIC    │    │USB    │    │SATA   ││                 │  │
│   │   │SSD x4 │    │10GbE  │    │3.0 Hub│    │Controller│                 │  │
│   │   │(Gen4) │    │       │    │       │    │       ││                 │  │
│   │   └───┬───┘    └───┬───┘    └───┬───┘    └───┬───┘│                 │  │
│   │       │            │            │            │    │                 │  │
│   │   ┌───┴───┐    ┌───┴───┐    ┌───┴───┐    ┌───┴───┐                │  │
│   │   │SSD 0  │    │RJ45   │    │USB-A  │    │SATA 0 │                │  │
│   │   │SSD 1  │    │       │    │USB-C  │    │SATA 1 │                │  │
│   │   │...    │    │       │    │       │    │...    │                │  │
│   │   └───────┘    └───────┘    └───────┘    └───────┘                │  │
│   │                                                                   │  │
│   └───────────────────────────────────────────────────────────────────┘  │
│                                                                             │
│   带宽演进:                                                                 │
│   PCIe 1.0 (2003): 2.5 GT/s, x16 = 8 GB/s                                  │
│   PCIe 2.0 (2007): 5.0 GT/s, x16 = 16 GB/s                                 │
│   PCIe 3.0 (2010): 8.0 GT/s, x16 = 32 GB/s                                 │
│   PCIe 4.0 (2017): 16.0 GT/s, x16 = 64 GB/s                                │
│   PCIe 5.0 (2019): 32.0 GT/s, x16 = 128 GB/s                               │
│   PCIe 6.0 (2022): 64.0 GT/s (PAM4), x16 = 256 GB/s                        │
│   PCIe 7.0 (2025): 128.0 GT/s, x16 = 512 GB/s (规划中)                      │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 1.2 PCIe vs 传统PCI

| 特性 | 传统PCI | PCIe |
|-----|---------|------|
| 拓扑 | 共享总线 | 点对点交换 |
| 信号 | 并行 | 串行 (差分对) |
| 时钟 | 33/66 MHz | 嵌入式时钟 |
| 带宽扩展 | 固定 | 通道数可扩展 (x1-x16) |
| 热插拔 | 不支持 | 原生支持 |
| 电源管理 | 有限 | 高级ASPM |
| 错误处理 | 基本 | 高级ECRC/AER |
| 虚拟化 | 无 | SR-IOV支持 |

---

## 2. PCIe架构概述

### 2.1 物理层拓扑

```
┌────────────────────────────────────────────────────────────────────────────┐
│                    PCIe物理层拓扑                                           │
├────────────────────────────────────────────────────────────────────────────┤
│                                                                            │
│   点对点连接模型:                                                          │
│   ┌───────────────────────────────────────────────────────────────────┐   │
│   │                                                                   │   │
│   │                    Root Complex (RC)                              │   │
│   │                        │                                          │   │
│   │           ┌────────────┼────────────┐                            │   │
│   │           │            │            │                            │   │
│   │           ▼            ▼            ▼                            │   │
│   │      ┌────────┐   ┌────────┐   ┌────────┐                       │   │
│   │      │ Port 0 │   │ Port 1 │   │ Port 2 │                       │   │
│   │      │ (x16)  │   │ (x4)   │   │ (x1)   │                       │   │
│   │      └───┬────┘   └───┬────┘   └───┬────┘                       │   │
│   │          │            │            │                             │   │
│   │          │ Link       │ Link       │ Link                       │   │
│   │          │ (双工)      │ (双工)     │ (双工)                      │   │
│   │          │            │            │                             │   │
│   │          ▼            ▼            ▼                             │   │
│   │      ┌────────┐   ┌────────┐   ┌────────┐                       │   │
│   │      │Endpoint│   │ Switch │   │Endpoint│                       │   │
│   │      │ (GPU)  │   │        │   │ (NIC)  │                       │   │
│   │      └────────┘   └───┬────┘   └────────┘                       │   │
│   │                       │                                          │   │
│   │              ┌────────┼────────┐                                 │   │
│   │              │        │        │                                 │   │
│   │              ▼        ▼        ▼                                 │   │
│   │          ┌──────┐ ┌──────┐ ┌──────┐                            │   │
│   │          │SSD 0 │ │SSD 1 │ │SSD 2 │                            │   │
│   │          └──────┘ └──────┘ └──────┘                            │   │
│   │                                                                   │   │
│   └───────────────────────────────────────────────────────────────────┘   │
│                                                                            │
│   Lane概念:                                                                │
│   ┌───────────────────────────────────────────────────────────────────┐   │
│   │                                                                   │   │
│   │   x1:  ┌─────┐        x4:  ┌─────────────┐                       │   │
│   │        │Tx+  │              │Tx0+ Tx1+ Tx2+ Tx3+│                 │   │
│   │        │Tx-  │              │Tx0- Tx1- Tx2- Tx3-│                 │   │
│   │        │Rx+  │              │Rx0+ Rx1+ Rx2+ Rx3+│                 │   │
│   │        │Rx-  │              │Rx0- Rx1- Rx2- Rx3-│                 │   │
│   │        └─────┘              └─────────────────┘                   │   │
│   │        1对差分发送              4对差分发送                        │   │
│   │        1对差分接收              4对差分接收                        │   │
│   │                                                                   │   │
│   │   x16显卡插槽: 16对差分信号 = 32条信号线                           │   │
│   │                                                                   │   │
│   └───────────────────────────────────────────────────────────────────┘   │
│                                                                            │
└────────────────────────────────────────────────────────────────────────────┘
```

### 2.2 配置空间

```
┌────────────────────────────────────────────────────────────────────────────┐
│                    PCIe配置空间                                             │
├────────────────────────────────────────────────────────────────────────────┤
│                                                                            │
│   PCI兼容配置空间 (256字节):                                               │
│   ┌───────────────────────────────────────────────────────────────────┐   │
│   │  Offset │  Register                    │  说明                    │   │
│   │  ───────┼──────────────────────────────┼─────────────────────────│   │
│   │  0x00   │  Vendor ID / Device ID       │  厂商/设备标识           │   │
│   │  0x04   │  Command / Status            │  命令/状态               │   │
│   │  0x08   │  Revision / Class Code       │  版本/类别               │   │
│   │  0x0C   │  Cache Line / Latency / BIST │  缓存行/延迟/BIST        │   │
│   │  0x10   │  BAR0 (Base Address 0)       │  基址寄存器0             │   │
│   │  0x14   │  BAR1                          (64位需要BAR0+BAR1)      │   │
│   │  0x18   │  BAR2                          (BAR2+BAR3)              │   │
│   │  0x1C   │  BAR3                                                   │   │
│   │  0x20   │  BAR4                          (BAR4+BAR5)              │   │
│   │  0x24   │  BAR5                                                   │   │
│   │  0x28   │  CardBus CIS Pointer                                    │   │
│   │  0x2C   │  Subsystem Vendor / ID       │  子系统标识              │   │
│   │  0x30   │  Expansion ROM Base          │  ROM基址                 │   │
│   │  0x34   │  Reserved / Cap Pointer      │  能力指针                │   │
│   │  0x3C   │  Interrupt Line / Pin / ...  │  中断配置                │   │
│   └───────────────────────────────────────────────────────────────────┘   │
│                                                                            │
│   PCIe扩展配置空间 (4KB):                                                  │
│   ┌───────────────────────────────────────────────────────────────────┐   │
│   │  0x100  │  PCI Power Management       │  电源管理能力            │   │
│   │  0x140  │  MSI (Message Signaled Int) │  MSI中断                 │   │
│   │  0x150  │  MSI-X                      │  扩展MSI中断             │   │
│   │  0x160  │  PCIe Capability            │  PCIe能力                │   │
│   │         │    - Link Cap/Control       │    链路能力/控制         │   │
│   │         │    - Device Cap/Control     │    设备能力/控制         │   │
│   │         │    - Root Cap/Control       │    根端口能力/控制       │   │
│   │  0x200  │  AER (Advanced Error Rpt)   │  高级错误报告            │   │
│   │  0x300  │  SR-IOV                     │  虚拟化                  │   │
│   │  0x400  │  ACS (Access Control Svcs)  │  访问控制                │   │
│   │  0x500  │  ATS (Address Translation)  │  地址转换服务            │   │
│   │  ...    │  ...                        │  更多能力                │   │
│   └───────────────────────────────────────────────────────────────────┘   │
│                                                                            │
│   配置空间访问方式:                                                        │
│   - Type 0: 用于Endpoint (Device 0, Function 0)                           │
│   - Type 1: 用于Bridge/Switch，包含次级/从属总线号                       │
│                                                                            │
└────────────────────────────────────────────────────────────────────────────┘
```

---

## 3. 分层结构

### 3.1 三层模型

```
┌────────────────────────────────────────────────────────────────────────────┐
│                    PCIe分层架构                                             │
├────────────────────────────────────────────────────────────────────────────┤
│                                                                            │
│   ┌─────────────────────────────────────────────────────────────────────┐ │
│   │                         应用层 (软件)                                │ │
│   │           驱动程序 → TLP生成 → 配置空间管理                         │ │
│   └─────────────────────────────────────────────────────────────────────┘ │
│                                    │                                      │
│   ═════════════════════════════════╪══════════════════════════════════    │
│                                    ▼                                      │
│   ┌─────────────────────────────────────────────────────────────────────┐ │
│   │  ┌───────────────────────────────────────────────────────────────┐  │ │
│   │  │                    事务层 (Transaction Layer)                  │  │ │
│   │  │                                                               │  │ │
│   │  │  功能:                                                        │  │ │
│   │  │  - TLP (Transaction Layer Packet) 组装/解析                   │  │ │
│   │  │  - 流量控制 (Flow Control)                                    │  │ │
│   │  │  - 虚拟通道 (Virtual Channel) 管理                            │  │ │
│   │  │  - 排序规则管理                                               │  │ │
│   │  │                                                               │  │ │
│   │  │  接口:                                                        │  │ │
│   │  │  TX: 上层 ──→ TLP Buffer ──→ 数据链路层                       │  │ │
│   │  │  RX: 数据链路层 ──→ TLP Buffer ──→ 上层                       │  │ │
│   │  │                                                               │  │ │
│   │  └───────────────────────────────────────────────────────────────┘  │ │
│   │                              │ │                                      │ │
│   │                              ▼ ▼                                      │ │
│   │  ┌───────────────────────────────────────────────────────────────┐  │ │
│   │  │                   数据链路层 (Data Link Layer)                 │  │ │
│   │  │                                                               │  │ │
│   │  │  功能:                                                        │  │ │
│   │  │  - 链路管理 (Link Management)                                 │  │ │
│   │  │  - 数据完整性 (CRC生成/校验)                                  │  │ │
│   │  │  - 错误检测与重传 (ACK/NAK协议)                               │  │ │
│   │  │  - DLLP (Data Link Layer Packet) 处理                         │  │ │
│   │  │                                                               │  │ │
│   │  │  DLLP类型:                                                    │  │ │
│   │  │  - ACK/NAK:  TLP确认/否定确认                                 │  │ │
│   │  │  - InitFC1/2/3: 流量控制初始化                                │  │ │
│   │  │  - UpdateFC: 流量控制更新                                     │  │ │
│   │  │  - PM_Enter/L0s/L1/L2: 电源管理                               │  │ │
│   │  │                                                               │  │ │
│   │  └───────────────────────────────────────────────────────────────┘  │ │
│   │                              │ │                                      │ │
│   │                              ▼ ▼                                      │ │
│   │  ┌───────────────────────────────────────────────────────────────┐  │ │
│   │  │                     物理层 (Physical Layer)                    │  │ │
│   │  │                                                               │  │ │
│   │  │  逻辑子层:                                                    │  │ │
│   │  │  - 字节到符号编码 (8b/10b, 128b/130b)                         │  │ │
│   │  │  - 加扰 (Scrambling) 减少EMI                                  │  │ │
│   │  │  - 弹性缓冲 (Elastic Buffer) 时钟补偿                         │  │ │
│   │  │  - 链路训练与状态机 (LTSSM)                                   │  │ │
│   │  │                                                               │  │ │
│   │  │  电气子层:                                                    │  │ │
│   │  │  - 差分信号驱动/接收                                          │  │ │
│   │  │  - 均衡 (Equalization) Gen3+                                  │  │ │
│   │  │  - 时钟数据恢复 (CDR)                                         │  │ │
│   │  │                                                               │  │ │
│   │  └───────────────────────────────────────────────────────────────┘  │ │
│   │                                                                     │ │
│   └─────────────────────────────────────────────────────────────────────┘ │
│                                    │                                      │
│                                    ▼                                      │
│   ┌─────────────────────────────────────────────────────────────────────┐ │
│   │                         物理介质                                     │ │
│   │            差分信号对 (Tx+/Tx-, Rx+/Rx-)                             │ │
│   │                    PCB走线 / 线缆                                    │ │
│   └─────────────────────────────────────────────────────────────────────┘ │
│                                                                            │
└────────────────────────────────────────────────────────────────────────────┘
```

### 3.2 LTSSM状态机

```
┌────────────────────────────────────────────────────────────────────────────┐
│                    链路训练与状态机 (LTSSM)                                 │
├────────────────────────────────────────────────────────────────────────────┤
│                                                                            │
│   检测状态:                                                                │
│   ┌─────────────┐                                                          │
│   │  Detect.Quiet│  ──→ 上电后静默，等待稳定                              │
│   └──────┬──────┘                                                          │
│          │ 发送Beacon (可选)                                               │
│          ▼                                                                 │
│   ┌─────────────┐                                                          │
│   │ Detect.Active│  ──→ 检测对端是否存在                                   │
│   └──────┬──────┘                                                          │
│          │ 检测到对端                                                      │
│          ▼                                                                 │
│                                                                            │
│   轮询状态 (Polling) - 位锁定和符号锁定:                                   │
│   ┌─────────────┐     ┌─────────────┐     ┌─────────────┐                 │
│   │Polling.Active│────→│  Polling.   │────→│   Polling.  │                 │
│   │             │     │Compliance   │     │   Config    │                 │
│   └─────────────┘     └─────────────┘     └──────┬──────┘                 │
│                                                   │                        │
│   配置状态 (Configuration) - 链路宽度和极性:                               │
│                                                   ▼                        │
│   ┌─────────────┐     ┌─────────────┐     ┌─────────────┐                 │
│   │  Config.    │←────│  Config.    │←────│   Config.   │                 │
│   │   Linkwidth │     │  Linkstart  │     │   Linkwait  │                 │
│   │   Accept    │     │  Equalization│    │   Start/End │                 │
│   └──────┬──────┘     └─────────────┘     └─────────────┘                 │
│          │                                                                 │
│          ▼                                                                 │
│   ┌─────────────┐                                                          │
│   │    L0       │  ←←← 正常操作状态 (完全训练完成)                         │
│   │  (Active)   │                                                          │
│   └──────┬──────┘                                                          │
│          │                                                                 │
│   电源管理状态:                                                             │
│          ├──→ L0s ──→ 快速退出低功耗 (几百ns)                              │
│          ├──→ L1 ───→ 更深低功耗 (几us退出)                                │
│          └──→ L2 ───→ 最深低功耗 (辅助电源)                                │
│                                                                            │
│   恢复状态 (Recovery) - 从错误恢复或改变速度:                              │
│          ├──→ Recovery.RcvrLock ──→ 重新获得位锁定                          │
│          ├──→ Recovery.RcvrCfg ───→ 重新配置                                │
│          └──→ Recovery.Idle ──────→ 准备返回L0                              │
│                                                                            │
│   禁用状态 (Disabled): 软件禁用链路                                        │
│                                                                            │
│   环回状态 (Loopback): 测试模式                                            │
│                                                                            │
│   热复位状态 (Hot Reset): 链路级复位                                       │
│                                                                            │
└────────────────────────────────────────────────────────────────────────────┘
```

---

## 4. TLP包格式

### 4.1 TLP通用格式

```
┌────────────────────────────────────────────────────────────────────────────┐
│                    TLP (Transaction Layer Packet) 格式                      │
├────────────────────────────────────────────────────────────────────────────┤
│                                                                            │
│   ┌─────────────────────────────────────────────────────────────────────┐ │
│   │                         TLP基本结构                                  │ │
│   │                                                                     │ │
│   │  ┌─────────────┬─────────────────────────┬─────────────────────────┐│ │
│   │  │   Header    │         Data            │        Digest(ECRC)     ││ │
│   │  │  (3-4 DW)   │       (0-1024 DW)       │        (0-1 DW)         ││ │
│   │  │             │    (Payload, 可选)      │        (可选)           ││ │
│   │  └─────────────┴─────────────────────────┴─────────────────────────┘│ │
│   │                                                                     │ │
│   │  DW = Double Word = 32 bits                                          │ │
│   │  最小TLP: 3 DW (Header only)                                         │ │
│   │  最大TLP: 4 + 1024 + 1 = 1029 DW (Gen3之前为3+256+1=260 DW)          │ │
│   │                                                                     │ │
│   └─────────────────────────────────────────────────────────────────────┘ │
│                                                                            │
│   通用Header格式 (3DW = 12字节):                                           │
│   ┌─────────────────────────────────────────────────────────────────────┐ │
│   │                                                                     │ │
│   │   Byte 0:                                                           │ │
│   │   ┌────┬────┬────┬────┬────┬────┬────┬────┐                        │ │
│   │   │ Fmt│ Fmt│ Type[4:0]  │ R  │TC[2:0]    │                        │ │
│   │   │ [1]│ [0]│            │    │          │                        │ │
│   │   └────┴────┴────────────┴────┴──────────┘                        │ │
│   │                                                                     │ │
│   │   Fmt (格式):                                                       │ │
│   │     000 = MRd (Memory Read, 3DW header, no data)                    │ │
│   │     001 = MRdLk (Memory Read Locked)                                │ │
│   │     010 = MWr (Memory Write, 3DW header, with data)                 │ │
│   │     100 = CfgRd0/1 (Config Read Type 0/1)                           │ │
│   │     101 = CfgWr0/1 (Config Write Type 0/1)                          │ │
│   │     010 = Completion (Cpl, 3DW header, with/without data)           │ │
│   │     110 = Msg/MsgD (Message/Message with Data)                      │ │
│   │                                                                     │ │
│   │   Byte 1:                                                           │ │
│   │   ┌────┬──────────┬──────────┬──────────┬──────────┐                │ │
│   │   │TH  │TD        │EP        │Attr[1:0] │AT[1:0]   │                │ │
│   │   │    │(Digest)  │(Poisoned)│          │          │                │ │
│   │   └────┴──────────┴──────────┴──────────┴──────────┘                │ │
│   │                                                                     │ │
│   │   Bytes 2-3: Length[9:0] (数据长度，以DW为单位)                      │ │
│   │                                                                     │ │
│   │   Bytes 4-7: Address[31:0] 或 Requester ID等 (取决于类型)            │ │
│   │                                                                     │ │
│   │   Bytes 8-11: Address[63:32] 或 Tag等 (4DW header)                   │ │
│   │                                                                     │ │
│   └─────────────────────────────────────────────────────────────────────┘ │
│                                                                            │
└────────────────────────────────────────────────────────────────────────────┘
```

### 4.2 具体TLP类型

```
┌────────────────────────────────────────────────────────────────────────────┐
│                    具体TLP类型详解                                          │
├────────────────────────────────────────────────────────────────────────────┤
│                                                                            │
│   1. Memory Read (MRd) - 内存读取请求:                                     │
│   ┌─────────────────────────────────────────────────────────────────────┐ │
│   │                                                                     │ │
│   │   DW 0: 0x00_0000xx  (Fmt=000, Type=00000)                          │ │
│   │   DW 1: Attr/Length                                                 │ │
│   │   DW 2: Address[31:2] | 00  (4字节对齐地址)                         │ │
│   │                                                                     │ │
│   │   响应: Completion with Data (CplD)                                 │ │
│   │                                                                     │ │
│   └─────────────────────────────────────────────────────────────────────┘ │
│                                                                            │
│   2. Memory Write (MWr) - 内存写入请求:                                    │
│   ┌─────────────────────────────────────────────────────────────────────┐ │
│   │                                                                     │ │
│   │   DW 0: 0x40_0000xx  (Fmt=010, Type=00000)                          │ │
│   │   DW 1: Attr/Length                                                 │ │
│   │   DW 2: Address[31:2] | 00                                          │ │
│   │   DW 3+: Data...                                                    │ │
│   │                                                                     │ │
│   │   注意: Memory Write不需要Completion响应 (Posted事务)                │ │
│   │                                                                     │ │
│   └─────────────────────────────────────────────────────────────────────┘ │
│                                                                            │
│   3. Configuration Read/Write - 配置空间访问:                              │
│   ┌─────────────────────────────────────────────────────────────────────┐ │
│   │                                                                     │ │
│   │   DW 0: 0x04_0000xx  (Fmt=100, Type=00100) - CfgRd0                 │ │
│   │         0x44_0000xx  (Fmt=101, Type=00100) - CfgWr0                 │ │
│   │   DW 1: Attr/Length                                                 │ │
│   │   DW 2: Requester ID | Tag | LastBE | FirstBE                       │ │
│   │   DW 3: Bus[7:0] | Device[4:0] | Func[2:0] | ExtReg[3:0] | Reg[5:0] │ │
│   │         │           │          │           │           │            │ │
│   │         总线号      设备号      功能号     扩展寄存器    寄存器偏移   │ │
│   │                                                                     │ │
│   │   配置空间地址 = Bus:Device:Function:Offset                         │ │
│   │                                                                     │ │
│   └─────────────────────────────────────────────────────────────────────┘ │
│                                                                            │
│   4. Completion (Cpl/CplD) - 完成包:                                       │
│   ┌─────────────────────────────────────────────────────────────────────┐ │
│   │                                                                     │ │
│   │   DW 0: 0x4A_0000xx  (Fmt=010, Type=01010) - CplD                   │ │
│   │   DW 1: Attr/Length                                                 │ │
│   │   DW 2: Completer ID | Status | BCM | Byte Count                    │ │
│   │   DW 3: Requester ID | Tag | R | Lower Address[6:0]                 │ │
│   │   DW 4+: Data...                                                    │ │
│   │                                                                     │ │
│   │   Status: 000 = Successful Completion (SC)                          │ │
│   │           001 = Unsupported Request (UR)                            │ │
│   │           010 = Config Retry Status (CRS)                           │ │
│   │           100 = Completer Abort (CA)                                │ │
│   │                                                                     │ │
│   └─────────────────────────────────────────────────────────────────────┘ │
│                                                                            │
│   5. Message (Msg/MsgD) - 消息传输:                                        │
│   ┌─────────────────────────────────────────────────────────────────────┐ │
│   │                                                                     │ │
│   │   DW 0: Fmt/Type = 01100xx (Msg) 或 01101xx (MsgD)                  │ │
│   │                                                                     │ │
│   │   消息代码 (Routing):                                               │ │
│   │   0x00 = Unlock                                                     │ │
│   │   0x20-0x23 = INTA-D Assert (Legacy中断)                            │ │
│   │   0x24-0x27 = INTA-D Deassert                                       │ │
│   │   0x30 = ERR_COR (Correctable Error)                                │ │
│   │   0x31 = ERR_NONFATAL                                               │ │
│   │   0x33 = ERR_FATAL                                                  │ │
│   │   0x50 = Set Slot Power Limit                                       │ │
│   │   0x7E-0x7F = Vendor Defined                                        │ │
│   │                                                                     │ │
│   │   MSI/MSI-X使用Msg/MsgD携带中断向量                                  │ │
│   │                                                                     │ │
│   └─────────────────────────────────────────────────────────────────────┘ │
│                                                                            │
└────────────────────────────────────────────────────────────────────────────┘
```

### 4.3 事务排序规则

```c
/*
 * PCIe事务排序规则 (Ordering Rules)
 * 确保正确性和性能
 */

// 事务类别:
// 1. Posted (P): MWr, Msg - 无需响应
// 2. Non-Posted (NP): MRd, IORd, CfgRd - 需要完成包
// 3. Completion (Cpl): Cpl, CplD - 响应包

// 排序规则表 (简化):
//           | 后续 P  | 后续 NP | 后续 Cpl
// 前序 P    |  Y(1)   |   Y     |   Y
// 前序 NP   |  N(2)   |   Y     |   Y
// 前序 Cpl  |  Y      |   Y     |   Y

// (1) 允许，但可能乱序 ( relaxed ordering )
// (2) 不允许，避免死锁

// Linux PCIe驱动中的排序考虑
static inline void pcie_set_relaxed_ordering(struct pci_dev *pdev, bool enable)
{
    u16 old, new;

    // 修改设备控制寄存器的 relaxed ordering 位
    pcie_capability_read_word(pdev, PCI_EXP_DEVCTL, &old);

    new = old;
    if (enable)
        new |= PCI_EXP_DEVCTL_RELAX_EN;
    else
        new &= ~PCI_EXP_DEVCTL_RELAX_EN;

    if (new != old)
        pcie_capability_write_word(pdev, PCI_EXP_DEVCTL, new);
}

// 流量控制 (Flow Control)
// 每个VC有6个独立的FC信用:
// - Posted Header (PH)
// - Posted Data (PD)
// - Non-Posted Header (NPH)
// - Non-Posted Data (NPD)
// - Completion Header (CPLH)
// - Completion Data (CPLD)

// 发送前必须检查信用可用性
struct pcie_fc_credits {
    u16 ph;     // Posted Header
    u16 pd;     // Posted Data
    u16 nph;    // Non-Posted Header
    u16 npd;    // Non-Posted Data
    u16 cplh;   // Completion Header
    u16 cpld;   // Completion Data
};
```

---

## 5. 枚举与配置

### 5.1 枚举过程

```
┌────────────────────────────────────────────────────────────────────────────┐
│                    PCIe枚举过程                                             │
├────────────────────────────────────────────────────────────────────────────┤
│                                                                            │
│   枚举目标: 发现所有PCIe设备，分配总线号、内存/IO资源                       │
│                                                                            │
│   ┌───────────────────────────────────────────────────────────────────┐   │
│   │                                                                   │   │
│   │  步骤1: 从Root Complex开始                                         │   │
│   │  ┌──────────┐                                                     │   │
│   │  │   RC     │  ← Bus 0, Device 0                                  │   │
│   │  │          │                                                     │   │
│   │  └───┬──────┘                                                     │   │
│   │      │                                                            │   │
│   │      │ 扫描Bus 0上所有Device (0-31)                               │   │
│   │      │                                                            │   │
│   │      ▼                                                            │   │
│   │  ┌──────────┐    发现Bridge/Switch                                │   │
│   │  │ Device 0 │    Vendor ID != 0xFFFF                               │   │
│   │  │ (Bridge) │                                                     │   │
│   │  └───┬──────┘                                                     │   │
│   │      │                                                            │   │
│   │  步骤2: 配置Bridge，分配次级总线号                                  │   │
│   │      │                                                            │   │
│   │      │ 写入:                                                       │   │
│   │      │   Primary Bus = 0                                          │   │
│   │      │   Secondary Bus = 1  (新总线)                               │   │
│   │      │   Subordinate Bus = 0xFF (临时)                             │   │
│   │      │                                                            │   │
│   │      ▼                                                            │   │
│   │  步骤3: 递归扫描次级总线                                            │   │
│   │      │                                                            │   │
│   │      │ 扫描Bus 1上所有Device                                       │   │
│   │      │                                                            │   │
│   │      ├──→ Endpoint (显卡) - 分配资源                               │   │
│   │      │                                                            │   │
│   │      └──→ 另一个Bridge                                            │   │
│   │           │                                                        │   │
│   │           │ 分配 Secondary Bus = 2                                 │   │
│   │           │                                                        │   │
│   │           └──→ 扫描Bus 2                                           │   │
│   │                │                                                   │   │
│   │                ├──→ NVMe SSD                                       │   │
│   │                └──→ USB控制器                                      │   │
│   │                                                                   │   │
│   │  步骤4: 更新Subordinate Bus号                                      │   │
│   │                                                                   │   │
│   │  Bus 0 Bridge: Subordinate = 2 (最远的总线)                         │   │
│   │  Bus 1 Bridge: Subordinate = 2                                     │   │
│   │                                                                   │   │
│   │  步骤5: 分配BAR资源                                                │   │
│   │                                                                   │   │
│   │  内存分配:                                                         │   │
│   │  ┌────────────────────────────────────────────────────────────┐  │   │
│   │  │ 0xE000_0000 - 0xE0FF_FFFF  │ GPU BAR0 (16MB)               │  │   │
│   │  │ 0xE100_0000 - 0xE10F_FFFF  │ NVMe BAR0 (1MB)               │  │   │
│   │  │ 0xE110_0000 - 0xE11F_FFFF  │ USB BAR0 (1MB)                │  │   │
│   │  │ ...                        │ ...                           │  │   │
│   │  └────────────────────────────────────────────────────────────┘  │   │
│   │                                                                   │   │
│   │  步骤6: 使能设备 (Command Register)                                 │   │
│   │  - Memory Space Enable                                            │   │
│   │  - Bus Master Enable (允许DMA)                                     │   │
│   │  - Interrupt Disable (如果使用MSI)                                 │   │
│   │                                                                   │   │
│   └───────────────────────────────────────────────────────────────────┘   │
│                                                                            │
└────────────────────────────────────────────────────────────────────────────┘
```

### 5.2 Linux枚举实现

```c
/*
 * Linux内核PCI枚举核心代码
 * drivers/pci/probe.c
 */

// 1. 扫描总线上的设备
static unsigned int pci_scan_child_bus(struct pci_bus *bus)
{
    unsigned int devfn, pass, max = bus->busn_res.start;
    struct pci_dev *dev;

    dev_dbg(&bus->dev, "scanning bus\n");

    // 遍历所有设备号 (0-31) 和功能号 (0-7)
    for (devfn = 0; devfn < 0x100; devfn += 8) {
        // 尝试读取Vendor ID
        u32 l;
        if (pci_bus_read_config_dword(bus, devfn, PCI_VENDOR_ID, &l))
            continue;

        // 0xFFFFFFFF 表示设备不存在
        if (l == 0xffffffff || l == 0x00000000 ||
            l == 0x0000ffff || l == 0xffff0000)
            continue;

        // 创建设备结构并扫描
        dev = pci_scan_single_device(bus, devfn);
        if (dev)
            dev->is_added = 1;
    }

    // 对桥设备进行递归扫描
    for (pass = 0; pass < 2; pass++)
        list_for_each_entry(dev, &bus->devices, bus_list)
            if (dev->hdr_type == PCI_HEADER_TYPE_BRIDGE ||
                dev->hdr_type == PCI_HEADER_TYPE_CARDBUS)
                if (dev->subordinate)
                    pci_scan_child_bus(dev->subordinate);

    // 分配总线号范围
    max = pci_scan_bridge(bus);

    return max;
}

// 2. 扫描桥设备
static int pci_scan_bridge(struct pci_bus *bus)
{
    struct pci_dev *dev;
    int pass, max;

    max = bus->busn_res.start;

    list_for_each_entry(dev, &bus->devices, bus_list) {
        // 只处理桥设备
        if (dev->hdr_type != PCI_HEADER_TYPE_BRIDGE &&
            dev->hdr_type != PCI_HEADER_TYPE_CARDBUS)
            continue;

        // 读取桥配置
        u16 bctl;
        u8 prim, sec, sub;

        pci_read_config_byte(dev, PCI_PRIMARY_BUS, &prim);
        pci_read_config_byte(dev, PCI_SECONDARY_BUS, &sec);
        pci_read_config_byte(dev, PCI_SUBORDINATE_BUS, &sub);

        // 如果是未配置的桥
        if (sec == 0) {
            // 分配新的次级总线号
            sec = ++max;

            // 写入配置
            pci_write_config_byte(dev, PCI_SECONDARY_BUS, sec);
            pci_write_config_byte(dev, PCI_SUBORDINATE_BUS, 0xFF);

            // 创建子总线结构
            child = pci_add_new_bus(bus, dev, sec);

            // 递归扫描
            max = pci_scan_child_bus(child);

            // 更新Subordinate Bus
            pci_write_config_byte(dev, PCI_SUBORDINATE_BUS, max);
        }
    }

    return max;
}

// 3. 分配BAR资源
static void pci_setup_device(struct pci_dev *dev)
{
    u32 orig, sz;
    int i, reg;

    // 遍历所有BAR (6个64位BAR或6个32位BAR)
    for (i = 0; i < PCI_NUM_RESOURCES; i++) {
        reg = PCI_BASE_ADDRESS_0 + (i * 4);

        // 读取原始值
        pci_read_config_dword(dev, reg, &orig);

        // 写入全1来探测大小
        pci_write_config_dword(dev, reg, 0xffffffff);
        pci_read_config_dword(dev, reg, &sz);
        pci_write_config_dword(dev, reg, orig);

        // 解码大小
        if (sz == 0 || sz == 0xffffffff)
            continue;

        if (sz & PCI_BASE_ADDRESS_SPACE_IO) {
            // I/O空间
            sz &= PCI_BASE_ADDRESS_IO_MASK;
            sz = (~sz + 1) & 0xffff;
            // 分配I/O资源...
        } else {
            // 内存空间
            sz &= PCI_BASE_ADDRESS_MEM_MASK;
            sz = ~sz + 1;

            // 检查64位BAR
            if ((orig & PCI_BASE_ADDRESS_MEM_TYPE_MASK) ==
                PCI_BASE_ADDRESS_MEM_TYPE_64) {
                // 需要读取下一个BAR作为高32位
                i++;
            }
            // 分配内存资源...
        }
    }
}

// 4. 使能设备
static void pci_enable_device_resources(struct pci_dev *dev)
{
    u16 cmd, old_cmd;

    pci_read_config_word(dev, PCI_COMMAND, &cmd);
    old_cmd = cmd;

    // 使能I/O空间
    if (dev->resource[PCI_ROM_RESOURCE].flags & IORESOURCE_IO)
        cmd |= PCI_COMMAND_IO;

    // 使能内存空间
    if (dev->resource[PCI_ROM_RESOURCE].flags & IORESOURCE_MEM)
        cmd |= PCI_COMMAND_MEMORY;

    // 使能总线主控 (DMA)
    cmd |= PCI_COMMAND_MASTER;

    if (cmd != old_cmd) {
        pci_write_config_word(dev, PCI_COMMAND, cmd);
    }
}
```

---

## 6. DMA与PCIe

### 6.1 PCIe DMA机制

```
┌────────────────────────────────────────────────────────────────────────────┐
│                    PCIe DMA机制                                             │
├────────────────────────────────────────────────────────────────────────────┤
│                                                                            │
│   设备DMA模型:                                                             │
│   ┌───────────────────────────────────────────────────────────────────┐   │
│   │                                                                   │   │
│   │   系统内存 (Host Memory)           PCIe设备                        │   │
│   │   ┌───────────────────┐            ┌───────────────────┐          │   │
│   │   │                   │            │                   │          │   │
│   │   │  ┌─────────────┐  │            │  ┌─────────────┐  │          │   │
│   │   │  │  RX Buffer  │  │◄───────────│  │ DMA Engine  │  │          │   │
│   │   │  │  (数据)     │  │  PCIe TLP  │  │  (发送方向) │  │          │   │
│   │   │  └─────────────┘  │   (MWr)    │  └─────────────┘  │          │   │
│   │   │         ↑         │            │         ↑         │          │   │
│   │   │         │         │            │         │         │          │   │
│   │   │  ┌─────────────┐  │            │  ┌─────────────┐  │          │   │
│   │   │  │  TX Buffer  │  │───────────→│  │ DMA Engine  │  │          │   │
│   │   │  │  (数据)     │  │  PCIe TLP  │  │  (接收方向) │  │          │   │
│   │   │  └─────────────┘  │   (MRd/    │  └─────────────┘  │          │   │
│   │   │                   │    CplD)   │                   │          │   │
│   │   │  ┌─────────────┐  │            │  ┌─────────────┐  │          │   │
│   │   │  │ Descriptor  │  │◄───────────│  │ Descriptor  │  │          │   │
│   │   │  │ Ring        │  │  PCIe TLP  │  │ Fetch       │  │          │   │
│   │   │  │             │  │   (MRd)    │  │             │  │          │   │
│   │   │  └─────────────┘  │            │  └─────────────┘  │          │   │
│   │   │                   │            │                   │          │   │
│   │   └───────────────────┘            └───────────────────┘          │   │
│   │                                                                   │   │
│   │   DMA操作类型:                                                    │   │
│   │   1. Host DMA (H2D/D2H): 系统内存 ↔ 设备                          │   │
│   │   2. Peer-to-Peer DMA: 设备 ↔ 设备 (无需CPU内存参与)               │   │
│   │                                                                   │   │
│   └───────────────────────────────────────────────────────────────────┘   │
│                                                                            │
│   地址转换 (IOMMU):                                                        │
│   ┌───────────────────────────────────────────────────────────────────┐   │
│   │                                                                   │   │
│   │   设备使用IOVA (I/O Virtual Address)                                │   │
│   │                                                                   │   │
│   │   设备DMA地址 → IOMMU → 物理地址                                    │   │
│   │                                                                   │   │
│   │   0xDEAD_0000    ┌──────────┐    0x1234_0000                       │   │
│   │   ┌──────────┐   │  IOMMU   │   ┌──────────┐                      │   │
│   │   │ Device   │──→│  Page    │──→│ Physical │                      │   │
│   │   │ DMA      │   │  Table   │   │ Memory   │                      │   │
│   │   └──────────┘   └──────────┘   └──────────┘                      │   │
│   │                                                                   │   │
│   │   优势:                                                           │   │
│   │   - 设备无法直接访问任意物理内存 (安全)                              │   │
│   │   - 支持设备内存碎片整理                                             │   │
│   │   - 支持设备热迁移                                                   │   │
│   │                                                                   │   │
│   └───────────────────────────────────────────────────────────────────┘   │
│                                                                            │
└────────────────────────────────────────────────────────────────────────────┘
```

### 6.2 Linux PCIe DMA编程

```c
/*
 * Linux PCIe设备DMA编程
 */

struct my_pcie_dev {
    struct pci_dev *pdev;
    void __iomem *bar0_base;

    // DMA相关
    struct dma_pool *desc_pool;
    struct device *dma_dev;

    // 描述符环
    struct my_dma_desc *desc_ring;
    dma_addr_t desc_dma;
    int desc_count;

    // 缓冲区
    void *tx_buf;
    dma_addr_t tx_dma;
    size_t buf_size;
};

// 1. 设备初始化
static int my_pcie_probe(struct pci_dev *pdev, const struct pci_device_id *id)
{
    struct my_pcie_dev *dev;
    int err;

    dev = kzalloc(sizeof(*dev), GFP_KERNEL);
    if (!dev)
        return -ENOMEM;

    dev->pdev = pdev;
    dev->dma_dev = &pdev->dev;

    // 启用PCI设备
    err = pci_enable_device(pdev);
    if (err) {
        dev_err(&pdev->dev, "Cannot enable PCI device\n");
        goto err_free;
    }

    // 请求BAR区域
    err = pci_request_regions(pdev, DRIVER_NAME);
    if (err) {
        dev_err(&pdev->dev, "Cannot obtain PCI resources\n");
        goto err_disable;
    }

    // 映射BAR0
    dev->bar0_base = pci_iomap(pdev, 0, pci_resource_len(pdev, 0));
    if (!dev->bar0_base) {
        err = -ENOMEM;
        goto err_release;
    }

    // 设置DMA掩码
    err = dma_set_mask_and_coherent(&pdev->dev, DMA_BIT_MASK(64));
    if (err) {
        err = dma_set_mask_and_coherent(&pdev->dev, DMA_BIT_MASK(32));
        if (err) {
            dev_err(&pdev->dev, "Cannot set DMA mask\n");
            goto err_unmap;
        }
    }

    // 使能PCIe relaxed ordering (如果需要)
    pcie_capability_set_word(pdev, PCI_EXP_DEVCTL,
                              PCI_EXP_DEVCTL_RELAX_EN);

    // 使能设备作为总线主控
    pci_set_master(pdev);

    // 分配DMA缓冲区
    dev->buf_size = 4096;
    dev->tx_buf = dma_alloc_coherent(&pdev->dev, dev->buf_size,
                                      &dev->tx_dma, GFP_KERNEL);
    if (!dev->tx_buf) {
        err = -ENOMEM;
        goto err_unmap;
    }

    // 创建DMA描述符池
    dev->desc_pool = dma_pool_create("my_desc", &pdev->dev,
                                      sizeof(struct my_dma_desc),
                                      64, 0);
    if (!dev->desc_pool) {
        err = -ENOMEM;
        goto err_free_dma;
    }

    // 分配描述符环
    dev->desc_count = 256;
    dev->desc_ring = dma_alloc_coherent(&pdev->dev,
                                         sizeof(struct my_dma_desc) * dev->desc_count,
                                         &dev->desc_dma, GFP_KERNEL);
    if (!dev->desc_ring) {
        err = -ENOMEM;
        goto err_pool;
    }

    // 配置设备描述符环寄存器
    // 写入描述符环基址到设备
    writel(lower_32_bits(dev->desc_dma),
           dev->bar0_base + REG_DESC_BASE_LO);
    writel(upper_32_bits(dev->desc_dma),
           dev->bar0_base + REG_DESC_BASE_HI);
    writel(dev->desc_count, dev->bar0_base + REG_DESC_COUNT);

    dev_info(&pdev->dev, "PCIe device initialized\n");
    dev_info(&pdev->dev, "  BAR0: %pR\n", &pdev->resource[0]);
    dev_info(&pdev->dev, "  DMA buffer: %pad\n", &dev->tx_dma);
    dev_info(&pdev->dev, "  Desc ring: %pad\n", &dev->desc_dma);

    return 0;

err_pool:
    dma_pool_destroy(dev->desc_pool);
err_free_dma:
    dma_free_coherent(&pdev->dev, dev->buf_size, dev->tx_buf, dev->tx_dma);
err_unmap:
    pci_iounmap(pdev, dev->bar0_base);
err_release:
    pci_release_regions(pdev);
err_disable:
    pci_disable_device(pdev);
err_free:
    kfree(dev);
    return err;
}

// 2. 提交DMA传输
static int submit_pcie_dma(struct my_pcie_dev *dev, dma_addr_t src,
                            dma_addr_t dst, size_t len)
{
    struct my_dma_desc *desc;
    u32 tail;

    // 获取下一个描述符位置
    tail = readl(dev->bar0_base + REG_DESC_TAIL);
    desc = &dev->desc_ring[tail];

    // 填充描述符
    desc->src_addr = cpu_to_le64(src);
    desc->dst_addr = cpu_to_le64(dst);
    desc->length = cpu_to_le32(len);
    desc->control = cpu_to_le32(DESC_CTRL_VALID | DESC_CTRL_IRQ);

    // 同步描述符到设备
    dma_wmb();

    // 更新尾指针通知设备
    tail = (tail + 1) % dev->desc_count;
    writel(tail, dev->bar0_base + REG_DESC_TAIL);

    return 0;
}

// 3. 清理和移除
static void my_pcie_remove(struct pci_dev *pdev)
{
    struct my_pcie_dev *dev = pci_get_drvdata(pdev);

    // 停止DMA
    writel(0, dev->bar0_base + REG_DMA_CTRL);

    // 释放资源
    dma_free_coherent(&pdev->dev,
                      sizeof(struct my_dma_desc) * dev->desc_count,
                      dev->desc_ring, dev->desc_dma);
    dma_pool_destroy(dev->desc_pool);
    dma_free_coherent(&pdev->dev, dev->buf_size, dev->tx_buf, dev->tx_dma);

    pci_iounmap(pdev, dev->bar0_base);
    pci_release_regions(pdev);
    pci_disable_device(pdev);

    kfree(dev);
}

// PCI设备ID表
static const struct pci_device_id my_pcie_ids[] = {
    { PCI_DEVICE(0x1234, 0x5678) },  // 厂商ID, 设备ID
    { 0, }
};
MODULE_DEVICE_TABLE(pci, my_pcie_ids);

static struct pci_driver my_pcie_driver = {
    .name = DRIVER_NAME,
    .id_table = my_pcie_ids,
    .probe = my_pcie_probe,
    .remove = my_pcie_remove,
};

module_pci_driver(my_pcie_driver);
```

---

## 7. 中断机制

### 7.1 PCIe中断类型

```
┌────────────────────────────────────────────────────────────────────────────┐
│                    PCIe中断机制                                             │
├────────────────────────────────────────────────────────────────────────────┤
│                                                                            │
│   1. Legacy Interrupt (INTx) - 传统中断:                                   │
│   ┌───────────────────────────────────────────────────────────────────┐   │
│   │                                                                   │   │
│   │   设备 ──INTA#──→ Root Complex ──→ 中断控制器 (8259/IO-APIC)       │   │
│   │                                                                   │   │
│   │   PCIe设备通过Message模拟INTx信号:                                 │   │
│   │   - Assert_INTA (Msg)                                             │   │
│   │   - Deassert_INTA (Msg)                                           │   │
│   │                                                                   │   │
│   │   缺点:                                                           │   │
│   │   - 共享中断线                                                    │   │
│   │   - 需要边沿触发/电平触发处理                                       │   │
│   │   - 性能较差                                                        │   │
│   │                                                                   │   │
│   └───────────────────────────────────────────────────────────────────┘   │
│                                                                            │
│   2. MSI (Message Signaled Interrupts) - 消息信号中断:                     │
│   ┌───────────────────────────────────────────────────────────────────┐   │
│   │                                                                   │   │
│   │   设备 ──Memory Write TLP──→ 指定地址 ──→ CPU中断                  │   │
│   │                                                                   │   │
│   │   配置:                                                           │   │
│   │   - MSI Capability在配置空间偏移0x50                              │   │
│   │   - Message Address: 中断写入的目标地址 (通常是APIC的寄存器)        │   │
│   │   - Message Data: 中断向量号                                       │   │
│   │   - Mask/Pending: 中断屏蔽和等待状态                               │   │
│   │                                                                   │   │
│   │   优点:                                                           │   │
│   │   - 每个设备可有多达32个中断向量                                   │   │
│   │   - 无共享中断问题                                                  │   │
│   │   - 更快的响应                                                      │   │
│   │                                                                   │   │
│   └───────────────────────────────────────────────────────────────────┘   │
│                                                                            │
│   3. MSI-X (Extended MSI) - 扩展消息信号中断:                              │
│   ┌───────────────────────────────────────────────────────────────────┐   │
│   │                                                                   │   │
│   │   支持多达2048个中断向量                                          │   │
│   │                                                                   │   │
│   │   结构:                                                           │   │
│   │   - MSI-X Capability在配置空间偏移0xB0                            │   │
│   │   - Table Offset: 指向MSI-X表的位置                                │   │
│   │   - PBA Offset: Pending Bit Array                                  │   │
│   │                                                                   │   │
│   │   MSI-X表条目 (16字节):                                           │   │
│   │   ┌────────────────┬────────────────┬────────────┬────────────┐   │   │
│   │   │ Message Address│ Message Address│  Message   │  Vector    │   │   │
│   │   │    Low 32bit   │   High 32bit   │   Data     │   Control  │   │   │
│   │   └────────────────┴────────────────┴────────────┴────────────┘   │   │
│   │                                                                   │   │
│   │   现代网卡通常每个队列一个MSI-X中断                                 │   │
│   │   - RX Queue 0 → MSI-X 0                                          │   │
│   │   - RX Queue 1 → MSI-X 1                                          │   │
│   │   - TX Queue 0 → MSI-X 2                                          │   │
│   │   - ...                                                           │   │
│   │                                                                   │   │
│   └───────────────────────────────────────────────────────────────────┘   │
│                                                                            │
└────────────────────────────────────────────────────────────────────────────┘
```

### 7.2 Linux MSI/MSI-X编程

```c
/*
 * Linux PCIe MSI/MSI-X编程
 */

// 1. 启用MSI或MSI-X
static int setup_interrupts(struct pci_dev *pdev, struct my_dev *dev)
{
    int ret, nvec;

    // 尝试MSI-X (首选)
    ret = pci_alloc_irq_vectors(pdev, 1, 8, PCI_IRQ_MSIX);
    if (ret < 0) {
        dev_info(&pdev->dev, "MSI-X not available, trying MSI\n");

        // 尝试MSI
        ret = pci_alloc_irq_vectors(pdev, 1, 4, PCI_IRQ_MSI);
        if (ret < 0) {
            dev_info(&pdev->dev, "MSI not available, using legacy IRQ\n");

            // 使用传统中断
            ret = pci_alloc_irq_vectors(pdev, 1, 1, PCI_IRQ_LEGACY);
            if (ret < 0)
                return ret;
        }
    }

    nvec = ret;
    dev_info(&pdev->dev, "Allocated %d interrupt vectors\n", nvec);

    // 注册中断处理函数
    for (int i = 0; i < nvec; i++) {
        ret = request_irq(pci_irq_vector(pdev, i), my_irq_handler,
                          0, DRIVER_NAME, dev);
        if (ret) {
            dev_err(&pdev->dev, "Failed to request IRQ %d\n", i);
            goto err_free_irq;
        }
    }

    return 0;

err_free_irq:
    while (--i >= 0)
        free_irq(pci_irq_vector(pdev, i), dev);
    pci_free_irq_vectors(pdev);
    return ret;
}

// 2. 中断处理函数
static irqreturn_t my_irq_handler(int irq, void *data)
{
    struct my_dev *dev = data;
    u32 status;

    // 读取中断状态
    status = readl(dev->bar0 + REG_INT_STATUS);

    if (!status)
        return IRQ_NONE;  // 不是我们的中断

    // 处理RX完成
    if (status & INT_RX_DONE) {
        napi_schedule(&dev->napi);
    }

    // 处理TX完成
    if (status & INT_TX_DONE) {
        // 清理已发送的描述符
        // ...
    }

    // 清除中断状态
    writel(status, dev->bar0 + REG_INT_STATUS);

    return IRQ_HANDLED;
}

// 3. 清理中断
static void cleanup_interrupts(struct pci_dev *pdev, struct my_dev *dev)
{
    int i, nvec = pci_msix_vec_count(pdev);

    if (nvec <= 0)
        nvec = pci_msi_vec_count(pdev);
    if (nvec <= 0)
        nvec = 1;

    for (i = 0; i < nvec; i++)
        free_irq(pci_irq_vector(pdev, i), dev);

    pci_free_irq_vectors(pdev);
}

// 4. 中断亲和性设置
static int set_irq_affinity(struct pci_dev *pdev, int vector, int cpu)
{
    int irq = pci_irq_vector(pdev, vector);

    return irq_set_affinity_hint(irq, cpumask_of(cpu));
}
```

---

## 8. 电源管理

### 8.1 PCIe电源状态

```
┌────────────────────────────────────────────────────────────────────────────┐
│                    PCIe电源管理状态                                         │
├────────────────────────────────────────────────────────────────────────────┤
│                                                                            │
│   链路电源状态:                                                            │
│   ┌───────────────────────────────────────────────────────────────────┐   │
│   │                                                                   │   │
│   │   L0  ──────→ 全活动状态，正常操作                                 │   │
│   │    │                                                              │   │
│   │    ├──→ L0s ───→ 快速退出低功耗，恢复时间 <1μs                    │   │
│   │    │   (仅物理层空闲)                                              │   │
│   │    │                                                              │   │
│   │    ├──→ L1  ───→ 更深低功耗，恢复时间 1-2μs                       │   │
│   │    │   (关断部分电路)                                              │   │
│   │    │                                                              │   │
│   │    └──→ L2/L3 → 最深低功耗，恢复时间毫秒级                        │   │
│   │        (仅保留唤醒逻辑，需要辅助电源)                               │   │
│   │                                                                   │   │
│   │   ASPM (Active State Power Management):                           │   │
│   │   - L0s: 硬件自动控制，进入/退出快                                 │   │
│   │   - L1: 软件控制，更深节能                                         │   │
│   │                                                                   │   │
│   └───────────────────────────────────────────────────────────────────┘   │
│                                                                            │
│   设备电源状态 (PCI PM):                                                   │
│   ┌───────────────────────────────────────────────────────────────────┐   │
│   │                                                                   │   │
│   │   D0  ──────→ 全功率状态                                           │   │
│   │   D0_uninit      未初始化                                          │   │
│   │   D0_active      活动操作                                          │   │
│   │                                                                   │   │
│   │   D1  ──────→ 轻量低功耗                                           │   │
│   │                保留上下文，唤醒快                                   │   │
│   │                                                                   │   │
│   │   D2  ──────→ 更深低功耗                                           │   │
│   │                可能需要重新初始化                                   │   │
│   │                                                                   │   │
│   │   D3hot ────→ 软关断                                               │   │
│   │                保留PCI配置空间                                      │   │
│   │                                                                   │   │
│   │   D3cold ───→ 完全关断                                             │   │
│   │                仅唤醒逻辑工作 (需要aux电源)                         │   │
│   │                                                                   │   │
│   └───────────────────────────────────────────────────────────────────┘   │
│                                                                            │
│   电源预算管理:                                                            │
│   ┌───────────────────────────────────────────────────────────────────┐   │
│   │                                                                   │   │
│   │   Slot Power Limit: 插槽功率限制                                   │   │
│   │   - PCIe卡不能超过此限制                                           │   │
│   │   - 通过Set_Slot_Power_Limit消息配置                               │   │
│   │                                                                   │   │
│   │   典型值:                                                          │   │
│   │   - x1 插槽: 10W-25W                                              │   │
│   │   - x4 插槽: 25W-50W                                              │   │
│   │   - x8/x16 插槽: 75W-300W (需额外供电)                             │   │
│   │                                                                   │   │
│   └───────────────────────────────────────────────────────────────────┘   │
│                                                                            │
└────────────────────────────────────────────────────────────────────────────┘
```

### 8.2 Linux电源管理

```c
/*
 * Linux PCIe电源管理
 */

// 1. 启用ASPM
static void enable_aspm(struct pci_dev *pdev)
{
    struct pcie_link_state *link = pdev->link_state;

    // 设置ASPM策略
    if (link) {
        pcie_config_aspm_link(link, ASPM_L0S_L1);
    }
}

// 2. 挂起/恢复回调
static int my_pcie_suspend(struct device *dev)
{
    struct pci_dev *pdev = to_pci_dev(dev);
    struct my_dev *mydev = pci_get_drvdata(pdev);

    // 停止设备操作
    my_stop_device(mydev);

    // 保存设备状态
    pci_save_state(pdev);

    // 设置D3hot状态
    pci_set_power_state(pdev, PCI_D3hot);

    // 禁用设备
    pci_disable_device(pdev);

    // 设置唤醒 (如果需要)
    if (device_may_wakeup(dev))
        pci_enable_wake(pdev, PCI_D3hot, 1);

    return 0;
}

static int my_pcie_resume(struct device *dev)
{
    struct pci_dev *pdev = to_pci_dev(dev);
    struct my_dev *mydev = pci_get_drvdata(pdev);
    int err;

    // 禁用唤醒
    if (device_may_wakeup(dev))
        pci_enable_wake(pdev, PCI_D3cold, 0);

    // 重新启用设备
    err = pci_enable_device(pdev);
    if (err)
        return err;

    pci_set_master(pdev);

    // 恢复设备状态
    pci_restore_state(pdev);

    // 重新初始化设备
    my_reinit_device(mydev);

    return 0;
}

static const struct dev_pm_ops my_pcie_pm_ops = {
    .suspend = my_pcie_suspend,
    .resume = my_pcie_resume,
    .freeze = my_pcie_suspend,
    .thaw = my_pcie_resume,
    .poweroff = my_pcie_suspend,
    .restore = my_pcie_resume,
};

// 3. 运行时电源管理
static int my_pcie_runtime_suspend(struct device *dev)
{
    struct pci_dev *pdev = to_pci_dev(dev);

    // 进入D3hot
    pci_save_state(pdev);
    pci_set_power_state(pdev, PCI_D3hot);

    return 0;
}

static int my_pcie_runtime_resume(struct device *dev)
{
    struct pci_dev *pdev = to_pci_dev(dev);

    // 恢复到D0
    pci_set_power_state(pdev, PCI_D0);
    pci_restore_state(pdev);

    return 0;
}

static const struct dev_pm_ops my_pcie_runtime_pm_ops = {
    SET_RUNTIME_PM_OPS(my_pcie_runtime_suspend,
                       my_pcie_runtime_resume,
                       NULL)
};
```

---

## 9. Linux PCIe驱动

### 9.1 完整PCIe驱动框架

```c
/*
 * 完整PCIe设备驱动示例
 */

#include <linux/module.h>
#include <linux/pci.h>
#include <linux/netdevice.h>
#include <linux/etherdevice.h>
#include <linux/dma-mapping.h>
#include <linux/interrupt.h>
#include <linux/ethtool.h>

#define DRIVER_NAME "my_pcie_nic"
#define BAR0_SIZE (4 * 1024)
#define TX_RING_SIZE 256
#define RX_RING_SIZE 256

// 设备寄存器定义
#define REG_DEVICE_CTRL     0x00
#define REG_DEVICE_STATUS   0x04
#define REG_MAC_ADDR_LOW    0x08
#define REG_MAC_ADDR_HIGH   0x0C
#define REG_TX_DESC_BASE    0x10
#define REG_TX_DESC_TAIL    0x18
#define REG_RX_DESC_BASE    0x20
#define REG_RX_DESC_TAIL    0x28
#define REG_INT_MASK        0x30
#define REG_INT_STATUS      0x34

#define CTRL_TX_EN          BIT(0)
#define CTRL_RX_EN          BIT(1)
#define CTRL_RESET          BIT(31)

#define INT_TX_COMPLETE     BIT(0)
#define INT_RX_COMPLETE     BIT(1)
#define INT_LINK_CHANGE     BIT(2)

// 描述符结构
struct my_desc {
    u64 buffer_addr;
    u16 length;
    u16 csum;
    u8 status;
    u8 errors;
    u16 vlan;
} __packed;

// 私有数据结构
struct my_priv {
    struct pci_dev *pdev;
    struct net_device *netdev;

    // MMIO区域
    void __iomem *mmio_base;

    // 描述符环
    struct my_desc *tx_ring;
    struct my_desc *rx_ring;
    dma_addr_t tx_ring_dma;
    dma_addr_t rx_ring_dma;

    // 缓冲区
    struct sk_buff *tx_skb[TX_RING_SIZE];
    struct sk_buff *rx_skb[RX_RING_SIZE];

    // 索引
    u16 tx_head;
    u16 tx_tail;
    u16 rx_tail;

    // NAPI
    struct napi_struct napi;

    // 统计
    u64 packets;
    u64 bytes;
    u64 dropped;
};

// 寄存器访问辅助函数
static inline u32 my_readl(struct my_priv *priv, u32 reg)
{
    return readl(priv->mmio_base + reg);
}

static inline void my_writel(struct my_priv *priv, u32 reg, u32 val)
{
    writel(val, priv->mmio_base + reg);
}

// 分配接收缓冲区
static int my_alloc_rx_buffers(struct my_priv *priv)
{
    int i;

    for (i = 0; i < RX_RING_SIZE; i++) {
        struct sk_buff *skb;
        dma_addr_t dma;

        skb = netdev_alloc_skb(priv->netdev, priv->netdev->mtu + ETH_HLEN);
        if (!skb)
            return -ENOMEM;

        dma = dma_map_single(&priv->pdev->dev, skb->data,
                              priv->netdev->mtu + ETH_HLEN,
                              DMA_FROM_DEVICE);
        if (dma_mapping_error(&priv->pdev->dev, dma)) {
            dev_kfree_skb(skb);
            return -ENOMEM;
        }

        priv->rx_skb[i] = skb;
        priv->rx_ring[i].buffer_addr = cpu_to_le64(dma);
        priv->rx_ring[i].status = 0;
    }

    return 0;
}

// 设备初始化
static int my_hw_init(struct my_priv *priv)
{
    u32 val;
    int err;

    // 重置设备
    my_writel(priv, REG_DEVICE_CTRL, CTRL_RESET);
    msleep(10);

    // 等待重置完成
    do {
        val = my_readl(priv, REG_DEVICE_CTRL);
    } while (val & CTRL_RESET);

    // 分配描述符环
    priv->tx_ring = dma_alloc_coherent(&priv->pdev->dev,
                                        sizeof(struct my_desc) * TX_RING_SIZE,
                                        &priv->tx_ring_dma, GFP_KERNEL);
    priv->rx_ring = dma_alloc_coherent(&priv->pdev->dev,
                                        sizeof(struct my_desc) * RX_RING_SIZE,
                                        &priv->rx_ring_dma, GFP_KERNEL);
    if (!priv->tx_ring || !priv->rx_ring)
        return -ENOMEM;

    // 分配RX缓冲区
    err = my_alloc_rx_buffers(priv);
    if (err)
        return err;

    // 配置描述符基址
    my_writel(priv, REG_TX_DESC_BASE, lower_32_bits(priv->tx_ring_dma));
    my_writel(priv, REG_TX_DESC_BASE + 4, upper_32_bits(priv->tx_ring_dma));
    my_writel(priv, REG_RX_DESC_BASE, lower_32_bits(priv->rx_ring_dma));
    my_writel(priv, REG_RX_DESC_BASE + 4, upper_32_bits(priv->rx_ring_dma));

    // 设置RX尾指针
    my_writel(priv, REG_RX_DESC_TAIL, RX_RING_SIZE - 1);
    priv->rx_tail = 0;
    priv->tx_head = priv->tx_tail = 0;

    // 配置MAC地址
    my_writel(priv, REG_MAC_ADDR_LOW,
              priv->netdev->dev_addr[0] |
              priv->netdev->dev_addr[1] << 8 |
              priv->netdev->dev_addr[2] << 16 |
              priv->netdev->dev_addr[3] << 24);
    my_writel(priv, REG_MAC_ADDR_HIGH,
              priv->netdev->dev_addr[4] |
              priv->netdev->dev_addr[5] << 8);

    // 使能中断
    my_writel(priv, REG_INT_MASK, INT_TX_COMPLETE | INT_RX_COMPLETE);

    return 0;
}

// 启动设备
static int my_open(struct net_device *netdev)
{
    struct my_priv *priv = netdev_priv(netdev);
    int err;

    err = my_hw_init(priv);
    if (err)
        return err;

    napi_enable(&priv->napi);
    netif_start_queue(netdev);

    // 使能TX/RX
    my_writel(priv, REG_DEVICE_CTRL, CTRL_TX_EN | CTRL_RX_EN);

    return 0;
}

// 停止设备
static int my_stop(struct net_device *netdev)
{
    struct my_priv *priv = netdev_priv(netdev);

    netif_stop_queue(netdev);
    napi_disable(&priv->napi);

    // 禁用TX/RX
    my_writel(priv, REG_DEVICE_CTRL, 0);

    // 释放缓冲区
    // ...

    return 0;
}

// 发送数据包
static netdev_tx_t my_start_xmit(struct sk_buff *skb, struct net_device *netdev)
{
    struct my_priv *priv = netdev_priv(netdev);
    u16 tx_head = priv->tx_head;
    struct my_desc *desc;
    dma_addr_t dma;

    // 检查描述符可用性
    if ((tx_head + 1) % TX_RING_SIZE == priv->tx_tail) {
        netif_stop_queue(netdev);
        return NETDEV_TX_BUSY;
    }

    // 映射SKB
    dma = dma_map_single(&priv->pdev->dev, skb->data, skb->len, DMA_TO_DEVICE);
    if (dma_mapping_error(&priv->pdev->dev, dma)) {
        dev_kfree_skb(skb);
        priv->stats.tx_dropped++;
        return NETDEV_TX_OK;
    }

    // 填充描述符
    desc = &priv->tx_ring[tx_head];
    desc->buffer_addr = cpu_to_le64(dma);
    desc->length = cpu_to_le16(skb->len);
    desc->status = 0;

    priv->tx_skb[tx_head] = skb;
    priv->tx_head = (tx_head + 1) % TX_RING_SIZE;

    // 更新尾指针
    my_writel(priv, REG_TX_DESC_TAIL, priv->tx_head);

    return NETDEV_TX_OK;
}

// NAPI轮询
static int my_poll(struct napi_struct *napi, int budget)
{
    struct my_priv *priv = container_of(napi, struct my_priv, napi);
    int work_done = 0;

    // 处理RX完成
    while (work_done < budget) {
        struct my_desc *desc = &priv->rx_ring[priv->rx_tail];

        if (!(desc->status & 0x01))  // 检查DD位
            break;

        // 处理接收的包
        struct sk_buff *skb = priv->rx_skb[priv->rx_tail];
        dma_addr_t dma = le64_to_cpu(desc->buffer_addr);
        u16 len = le16_to_cpu(desc->length);

        dma_unmap_single(&priv->pdev->dev, dma,
                          priv->netdev->mtu + ETH_HLEN, DMA_FROM_DEVICE);

        skb_put(skb, len);
        skb->protocol = eth_type_trans(skb, priv->netdev);

        napi_gro_receive(napi, skb);

        // 分配新的缓冲区
        skb = netdev_alloc_skb(priv->netdev, priv->netdev->mtu + ETH_HLEN);
        dma = dma_map_single(&priv->pdev->dev, skb->data,
                              priv->netdev->mtu + ETH_HLEN, DMA_FROM_DEVICE);

        priv->rx_skb[priv->rx_tail] = skb;
        desc->buffer_addr = cpu_to_le64(dma);
        desc->status = 0;

        priv->rx_tail = (priv->rx_tail + 1) % RX_RING_SIZE;
        work_done++;
    }

    // 更新RX尾指针
    my_writel(priv, REG_RX_DESC_TAIL,
              (priv->rx_tail - 1 + RX_RING_SIZE) % RX_RING_SIZE);

    if (work_done < budget)
        napi_complete_done(napi, work_done);

    return work_done;
}

// 中断处理
static irqreturn_t my_interrupt(int irq, void *data)
{
    struct my_priv *priv = data;
    u32 status;

    status = my_readl(priv, REG_INT_STATUS);
    if (!status)
        return IRQ_NONE;

    // 清除中断
    my_writel(priv, REG_INT_STATUS, status);

    if (status & INT_RX_COMPLETE) {
        my_writel(priv, REG_INT_MASK,
                  my_readl(priv, REG_INT_MASK) & ~INT_RX_COMPLETE);
        napi_schedule(&priv->napi);
    }

    if (status & INT_TX_COMPLETE) {
        // 清理发送描述符
        // ...
    }

    return IRQ_HANDLED;
}

// 网络设备操作
static const struct net_device_ops my_netdev_ops = {
    .ndo_open = my_open,
    .ndo_stop = my_stop,
    .ndo_start_xmit = my_start_xmit,
    .ndo_set_mac_address = eth_mac_addr,
    .ndo_validate_addr = eth_validate_addr,
};

// PCI探测
static int my_pcie_probe(struct pci_dev *pdev, const struct pci_device_id *id)
{
    struct net_device *netdev;
    struct my_priv *priv;
    int err;

    // 启用PCI设备
    err = pci_enable_device(pdev);
    if (err) {
        dev_err(&pdev->dev, "Cannot enable PCI device\n");
        return err;
    }

    // 请求BAR区域
    err = pci_request_regions(pdev, DRIVER_NAME);
    if (err) {
        dev_err(&pdev->dev, "Cannot obtain PCI resources\n");
        goto err_disable;
    }

    // 检查BAR0
    if (!(pci_resource_flags(pdev, 0) & IORESOURCE_MEM)) {
        dev_err(&pdev->dev, "BAR0 is not MMIO\n");
        err = -ENODEV;
        goto err_release;
    }

    // 分配网络设备
    netdev = alloc_etherdev(sizeof(*priv));
    if (!netdev) {
        err = -ENOMEM;
        goto err_release;
    }

    SET_NETDEV_DEV(netdev, &pdev->dev);
    netdev->netdev_ops = &my_netdev_ops;

    priv = netdev_priv(netdev);
    priv->pdev = pdev;
    priv->netdev = netdev;

    // 映射BAR0
    priv->mmio_base = pci_iomap(pdev, 0, pci_resource_len(pdev, 0));
    if (!priv->mmio_base) {
        err = -ENOMEM;
        goto err_free_netdev;
    }

    // 设置DMA
    err = dma_set_mask_and_coherent(&pdev->dev, DMA_BIT_MASK(64));
    if (err) {
        err = dma_set_mask_and_coherent(&pdev->dev, DMA_BIT_MASK(32));
        if (err)
            goto err_unmap;
    }

    pci_set_master(pdev);

    // 申请中断
    err = pci_alloc_irq_vectors(pdev, 1, 1, PCI_IRQ_ALL_TYPES);
    if (err < 0)
        goto err_unmap;

    err = request_irq(pci_irq_vector(pdev, 0), my_interrupt, 0,
                      DRIVER_NAME, priv);
    if (err)
        goto err_free_irq;

    // 初始化NAPI
    netif_napi_add(netdev, &priv->napi, my_poll, 64);

    // 生成MAC地址
    eth_hw_addr_random(netdev);

    // 注册网络设备
    err = register_netdev(netdev);
    if (err)
        goto err_napi;

    pci_set_drvdata(pdev, priv);

    dev_info(&pdev->dev, "My PCIe NIC driver loaded\n");

    return 0;

err_napi:
    netif_napi_del(&priv->napi);
    free_irq(pci_irq_vector(pdev, 0), priv);
err_free_irq:
    pci_free_irq_vectors(pdev);
err_unmap:
    pci_iounmap(pdev, priv->mmio_base);
err_free_netdev:
    free_netdev(netdev);
err_release:
    pci_release_regions(pdev);
err_disable:
    pci_disable_device(pdev);
    return err;
}

// PCI移除
static void my_pcie_remove(struct pci_dev *pdev)
{
    struct my_priv *priv = pci_get_drvdata(pdev);

    unregister_netdev(priv->netdev);
    netif_napi_del(&priv->napi);

    free_irq(pci_irq_vector(pdev, 0), priv);
    pci_free_irq_vectors(pdev);

    pci_iounmap(pdev, priv->mmio_base);
    pci_release_regions(pdev);
    pci_disable_device(pdev);

    free_netdev(priv->netdev);
}

static const struct pci_device_id my_pcie_ids[] = {
    { PCI_DEVICE(0x1234, 0x5678) },
    { }
};
MODULE_DEVICE_TABLE(pci, my_pcie_ids);

static struct pci_driver my_pcie_driver = {
    .name = DRIVER_NAME,
    .id_table = my_pcie_ids,
    .probe = my_pcie_probe,
    .remove = my_pcie_remove,
};

module_pci_driver(my_pcie_driver);

MODULE_LICENSE("GPL");
MODULE_DESCRIPTION("My PCIe NIC Driver");
MODULE_AUTHOR("Author");
```

---

## 10. Verilog实现

### 10.1 简化PCIe Endpoint

```verilog
//============================================================================
// 简化版PCIe Endpoint控制器
// 实现基本的TLP处理和DMA功能
//============================================================================

module pcie_endpoint (
    input  wire        pcie_clk,
    input  wire        pcie_rst_n,

    // 物理层接口 (简化)
    input  wire [127:0] rx_tlp_data,
    input  wire         rx_tlp_valid,
    output reg          rx_tlp_ready,
    output reg  [127:0] tx_tlp_data,
    output reg          tx_tlp_valid,
    input  wire         tx_tlp_ready,

    // 配置空间接口
    input  wire [11:0]  cfg_addr,
    input  wire [31:0]  cfg_wdata,
    output reg  [31:0]  cfg_rdata,
    input  wire         cfg_wr,
    input  wire         cfg_rd,

    // DMA接口
    output reg  [63:0]  dma_addr,
    output reg  [31:0]  dma_wdata,
    input  wire [31:0]  dma_rdata,
    output reg          dma_wr,
    output reg          dma_rd,
    input  wire         dma_ack,

    // 中断输出
    output reg          int_req,
    input  wire         int_ack
);

    // TLP类型定义
    localparam TLP_TYPE_MRD32  = 5'b00000;  // Memory Read 32-bit
    localparam TLP_TYPE_MRD64  = 5'b01000;  // Memory Read 64-bit
    localparam TLP_TYPE_MWR32  = 5'b10000;  // Memory Write 32-bit
    localparam TLP_TYPE_MWR64  = 5'b11000;  // Memory Write 64-bit
    localparam TLP_TYPE_CFGRD0 = 5'b00100;  // Config Read Type 0
    localparam TLP_TYPE_CFGWR0 = 5'b10100;  // Config Write Type 0
    localparam TLP_TYPE_CPL    = 5'b01010;  // Completion
    localparam TLP_TYPE_CPLD   = 5'b10010;  // Completion with Data

    // 内部状态机
    localparam STATE_IDLE      = 4'd0;
    localparam STATE_RX_PARSE  = 4'd1;
    localparam STATE_RX_DATA   = 4'd2;
    localparam STATE_CFG_READ  = 4'd3;
    localparam STATE_CFG_WRITE = 4'd4;
    localparam STATE_MEM_READ  = 4'd5;
    localparam STATE_MEM_WRITE = 4'd6;
    localparam STATE_TX_CPL    = 4'd7;
    localparam STATE_TX_DATA   = 4'd8;

    reg [3:0] state;
    reg [3:0] next_state;

    // 配置空间 (简化)
    reg [31:0] cfg_space [0:15];  // 64字节配置空间

    // BAR寄存器
    reg [31:0] bar0;
    reg [31:0] bar1;
    reg        bar0_enabled;
    reg        mem_space_en;
    reg        bus_master_en;

    // TLP解析寄存器
    reg [2:0]  tlp_fmt;
    reg [4:0]  tlp_type;
    reg [9:0]  tlp_len;
    reg [15:0] tlp_requester_id;
    reg [7:0]  tlp_tag;
    reg [6:0]  tlp_lower_addr;
    reg [31:0] tlp_addr32;
    reg [63:0] tlp_addr64;
    reg [31:0] tlp_data [0:7];  // 最大256字节
    reg [3:0]  tlp_data_cnt;

    // 初始化配置空间
    initial begin
        cfg_space[0] = 32'h12345678;  // Vendor ID, Device ID
        cfg_space[1] = 32'h00100000;  // Command, Status
        cfg_space[2] = 32'h02000000;  // Class Code, Revision
        cfg_space[3] = 32'h00000000;  // Header Type, etc
        cfg_space[4] = 32'h00000000;  // BAR0
        cfg_space[5] = 32'h00000000;  // BAR1
        cfg_space[6] = 32'h00000000;  // BAR2
        cfg_space[7] = 32'h00000000;  // BAR3
        bar0 = 32'h0;
        bar0_enabled = 1'b0;
    end

    // 状态机
    always @(posedge pcie_clk or negedge pcie_rst_n) begin
        if (!pcie_rst_n) begin
            state <= STATE_IDLE;
        end else begin
            state <= next_state;
        end
    end

    always @(*) begin
        next_state = state;

        case (state)
            STATE_IDLE: begin
                if (rx_tlp_valid)
                    next_state = STATE_RX_PARSE;
            end

            STATE_RX_PARSE: begin
                case (tlp_type)
                    TLP_TYPE_CFGRD0: next_state = STATE_CFG_READ;
                    TLP_TYPE_CFGWR0: next_state = STATE_CFG_WRITE;
                    TLP_TYPE_MRD32:  next_state = STATE_MEM_READ;
                    TLP_TYPE_MWR32:  next_state = STATE_MEM_WRITE;
                    default: next_state = STATE_IDLE;
                endcase
            end

            STATE_CFG_READ:  next_state = STATE_TX_CPL;
            STATE_CFG_WRITE: next_state = STATE_IDLE;
            STATE_MEM_READ:  next_state = STATE_TX_CPL;
            STATE_MEM_WRITE: next_state = STATE_IDLE;

            STATE_TX_CPL: begin
                if (tx_tlp_ready)
                    next_state = STATE_IDLE;
            end

            default: next_state = STATE_IDLE;
        endcase
    end

    // TLP解析
    always @(posedge pcie_clk or negedge pcie_rst_n) begin
        if (!pcie_rst_n) begin
            rx_tlp_ready <= 1'b0;
            tlp_data_cnt <= 4'd0;
        end else begin
            case (state)
                STATE_IDLE: begin
                    rx_tlp_ready <= 1'b1;
                    if (rx_tlp_valid) begin
                        // 解析Header
                        tlp_fmt <= rx_tlp_data[2:0];
                        tlp_type <= rx_tlp_data[7:3];
                        tlp_len <= rx_tlp_data[41:32];
                        tlp_requester_id <= rx_tlp_data[63:48];
                        tlp_tag <= rx_tlp_data[71:64];
                        tlp_lower_addr <= rx_tlp_data[102:96];

                        // 3DW或4DW header
                        if (rx_tlp_data[2] == 1'b0) begin
                            tlp_addr32 <= rx_tlp_data[95:64];
                        end else begin
                            tlp_addr64 <= {rx_tlp_data[127:96], rx_tlp_data[95:64]};
                        end

                        rx_tlp_ready <= 1'b0;
                    end
                end

                STATE_RX_DATA: begin
                    if (rx_tlp_valid) begin
                        // 存储数据
                        tlp_data[tlp_data_cnt] <= rx_tlp_data[31:0];
                        tlp_data_cnt <= tlp_data_cnt + 1;
                    end
                end
            endcase
        end
    end

    // 配置空间访问
    always @(posedge pcie_clk or negedge pcie_rst_n) begin
        if (!pcie_rst_n) begin
            cfg_rdata <= 32'h0;
        end else begin
            if (cfg_rd) begin
                cfg_rdata <= cfg_space[cfg_addr[5:2]];
            end else if (cfg_wr) begin
                cfg_space[cfg_addr[5:2]] <= cfg_wdata;

                // 更新BAR寄存器
                case (cfg_addr[5:2])
                    4'd4: bar0 <= cfg_wdata;
                    4'd1: begin
                        mem_space_en <= cfg_wdata[1];
                        bus_master_en <= cfg_wdata[2];
                    end
                endcase
            end
        end
    end

    // TLP处理
    always @(posedge pcie_clk or negedge pcie_rst_n) begin
        if (!pcie_rst_n) begin
            tx_tlp_valid <= 1'b0;
            dma_wr <= 1'b0;
            dma_rd <= 1'b0;
        end else begin
            tx_tlp_valid <= 1'b0;
            dma_wr <= 1'b0;
            dma_rd <= 1'b0;

            case (state)
                STATE_CFG_READ: begin
                    // 读取配置空间
                    // ...
                end

                STATE_CFG_WRITE: begin
                    // 写入配置空间 (在always块中处理)
                end

                STATE_MEM_READ: begin
                    if (bus_master_en) begin
                        // 发起DMA读
                        dma_addr <= {32'h0, tlp_addr32};
                        dma_rd <= 1'b1;
                    end
                end

                STATE_MEM_WRITE: begin
                    if (bus_master_en && mem_space_en) begin
                        // 发起DMA写
                        dma_addr <= {32'h0, tlp_addr32};
                        dma_wdata <= tlp_data[0];
                        dma_wr <= 1'b1;
                    end
                end

                STATE_TX_CPL: begin
                    if (tx_tlp_ready) begin
                        // 构建完成包
                        tx_tlp_data[127:96] <= {tlp_requester_id, tlp_tag, 8'h00};  // Requester ID, Tag
                        tx_tlp_data[95:64]  <= 32'h00000004;  // Completer ID, Status=0, BCM=0
                        tx_tlp_data[63:32]  <= {tlp_lower_addr, 25'h0};  // Lower Address
                        tx_tlp_data[31:0]   <= dma_rdata;  // 数据
                        tx_tlp_valid <= 1'b1;
                    end
                end
            endcase
        end
    end

endmodule


//============================================================================
// DMA描述符控制器
//============================================================================

module pcie_dma_ctrl (
    input  wire        clk,
    input  wire        rst_n,

    // PCIe TLP接口
    output reg  [127:0] tx_tlp_data,
    output reg          tx_tlp_valid,
    input  wire         tx_tlp_ready,
    input  wire [127:0] rx_tlp_data,
    input  wire         rx_tlp_valid,

    // 描述符接口
    input  wire [63:0]  desc_base,
    input  wire [15:0]  desc_count,
    input  wire [15:0]  desc_tail,
    output reg  [15:0]  desc_head,

    // 本地内存接口
    output reg  [31:0]  local_addr,
    input  wire [31:0]  local_rdata,
    output reg  [31:0]  local_wdata,
    output reg          local_rd,
    output reg          local_wr,
    input  wire         local_ack
);

    // 描述符格式
    typedef struct packed {
        reg [63:0] src_addr;
        reg [63:0] dst_addr;
        reg [31:0] length;
        reg [31:0] control;
    } desc_t;

    // 状态机
    localparam STATE_IDLE      = 4'd0;
    localparam STATE_FETCH     = 4'd1;
    localparam STATE_READ_DATA = 4'd2;
    localparam STATE_WRITE_TLP = 4'd3;
    localparam STATE_WRITE_ACK = 4'd4;
    localparam STATE_UPDATE    = 4'd5;

    reg [3:0] state;
    desc_t current_desc;
    reg [31:0] bytes_done;

    always @(posedge clk or negedge rst_n) begin
        if (!rst_n) begin
            state <= STATE_IDLE;
            desc_head <= 16'h0;
            tx_tlp_valid <= 1'b0;
            local_rd <= 1'b0;
            local_wr <= 1'b0;
        end else begin
            case (state)
                STATE_IDLE: begin
                    // 检查是否有新描述符
                    if (desc_head != desc_tail) begin
                        state <= STATE_FETCH;
                    end
                end

                STATE_FETCH: begin
                    // 从内存读取描述符
                    // (简化: 假设立即返回)
                    state <= STATE_READ_DATA;
                    bytes_done <= 32'h0;
                end

                STATE_READ_DATA: begin
                    // 读取源数据
                    local_addr <= current_desc.src_addr[31:0] + bytes_done;
                    local_rd <= 1'b1;
                    if (local_ack) begin
                        local_rd <= 1'b0;
                        state <= STATE_WRITE_TLP;
                    end
                end

                STATE_WRITE_TLP: begin
                    // 构建Memory Write TLP
                    if (tx_tlp_ready) begin
                        tx_tlp_data[127:96] <= {4'b0111, 5'b10000, 1'b0, 2'b00, 1'b0, 10'd1};  // Fmt, Type, Length=1
                        tx_tlp_data[95:64] <= {16'h0, 8'h00, 8'hFF};  // Requester ID, Tag, Last/First BE
                        tx_tlp_data[63:32] <= current_desc.dst_addr[31:0] + bytes_done;  // Address
                        tx_tlp_data[31:0]  <= local_rdata;  // Data
                        tx_tlp_valid <= 1'b1;
                        bytes_done <= bytes_done + 4;
                        state <= STATE_WRITE_ACK;
                    end
                end

                STATE_WRITE_ACK: begin
                    tx_tlp_valid <= 1'b0;
                    if (bytes_done >= current_desc.length)
                        state <= STATE_UPDATE;
                    else
                        state <= STATE_READ_DATA;
                end

                STATE_UPDATE: begin
                    desc_head <= (desc_head + 1) % desc_count;
                    state <= STATE_IDLE;
                end
            endcase
        end
    end

endmodule


//============================================================================
// PCIe Endpoint测试平台
//============================================================================

module tb_pcie_endpoint;

    reg         pcie_clk;
    reg         pcie_rst_n;
    reg  [127:0] rx_tlp_data;
    reg          rx_tlp_valid;
    wire         rx_tlp_ready;
    wire [127:0] tx_tlp_data;
    wire         tx_tlp_valid;
    reg          tx_tlp_ready;
    reg  [11:0]  cfg_addr;
    reg  [31:0]  cfg_wdata;
    wire [31:0]  cfg_rdata;
    reg          cfg_wr;
    reg          cfg_rd;

    // 实例化DUT
    pcie_endpoint dut (
        .pcie_clk(pcie_clk),
        .pcie_rst_n(pcie_rst_n),
        .rx_tlp_data(rx_tlp_data),
        .rx_tlp_valid(rx_tlp_valid),
        .rx_tlp_ready(rx_tlp_ready),
        .tx_tlp_data(tx_tlp_data),
        .tx_tlp_valid(tx_tlp_valid),
        .tx_tlp_ready(tx_tlp_ready),
        .cfg_addr(cfg_addr),
        .cfg_wdata(cfg_wdata),
        .cfg_rdata(cfg_rdata),
        .cfg_wr(cfg_wr),
        .cfg_rd(cfg_rd),
        .dma_addr(),
        .dma_wdata(),
        .dma_rdata(32'hDEAD_BEEF),
        .dma_wr(),
        .dma_rd(),
        .dma_ack(1'b1),
        .int_req(),
        .int_ack(1'b0)
    );

    // 时钟
    initial pcie_clk = 0;
    always #5 pcie_clk = ~pcie_clk;

    // 测试
    initial begin
        $display("============================================");
        $display("    PCIe Endpoint测试开始                  ");
        $display("============================================");

        // 初始化
        pcie_rst_n = 0;
        rx_tlp_data = 128'h0;
        rx_tlp_valid = 0;
        tx_tlp_ready = 1;
        cfg_addr = 0;
        cfg_wdata = 0;
        cfg_wr = 0;
        cfg_rd = 0;

        #20 pcie_rst_n = 1;
        #10;

        // 1. 读取配置空间 Vendor ID
        $display("\n[测试1] 读取Vendor ID/Device ID");
        cfg_addr = 12'h0;
        cfg_rd = 1;
        #10 cfg_rd = 0;
        #10;
        $display("  Vendor/Device ID = 0x%08X", cfg_rdata);

        // 2. 配置BAR0
        $display("\n[测试2] 配置BAR0");
        cfg_addr = 12'h10;  // BAR0 offset
        cfg_wdata = 32'hE000_0000;
        cfg_wr = 1;
        #10 cfg_wr = 0;

        // 使能总线主控
        cfg_addr = 12'h04;  // Command register
        cfg_wdata = 32'h0000_0006;  // Bus Master + Memory Space
        cfg_wr = 1;
        #10 cfg_wr = 0;

        $display("  BAR0 = 0x%08X", cfg_wdata);

        // 3. 发送MRd TLP
        $display("\n[测试3] 发送Memory Read TLP");
        rx_tlp_data[127:0] = {
            32'h0000_0000,      // 地址高32位
            32'hE000_0100,      // 地址低32位
            32'h0000_001F,      // Requester ID, Tag, Last/First BE
            32'h0000_0001       // Fmt=0, Type=0x00, Length=1
        };
        rx_tlp_valid = 1;
        #10 rx_tlp_valid = 0;

        wait(tx_tlp_valid);
        $display("  收到Completion TLP");
        $display("  Data = 0x%08X", tx_tlp_data[31:0]);
        #20;

        // 4. 发送MWr TLP
        $display("\n[测试4] 发送Memory Write TLP");
        rx_tlp_data[127:0] = {
            32'hCAFE_BABE,      // Data
            32'hE000_0200,      // Address
            32'h0000_00FF,      // Requester ID, Tag, Last/First BE
            32'h4000_0001       // Fmt=2, Type=0x00, Length=1
        };
        rx_tlp_valid = 1;
        #10 rx_tlp_valid = 0;
        #30;

        $display("  Memory Write完成");

        #50;

        $display("\n============================================");
        $display("    测试完成                              ");
        $display("============================================");

        $finish;
    end

    // 波形dump
    initial begin
        $dumpfile("pcie_endpoint.vcd");
        $dumpvars(0, tb_pcie_endpoint);
    end

endmodule
```

---

## 总结

PCIe是现代计算机系统中最重要的I/O互连技术。本文档详细覆盖了:

1. **PCIe架构**: 从物理层到应用层的完整架构
2. **分层结构**: 事务层、数据链路层、物理层的职责
3. **TLP包格式**: 各种事务类型的详细格式
4. **枚举与配置**: 系统如何发现和配置PCIe设备
5. **DMA机制**: PCIe设备如何直接访问系统内存
6. **中断机制**: Legacy、MSI、MSI-X中断的实现
7. **电源管理**: ASPM和各种电源状态
8. **Linux驱动**: 完整的PCIe设备驱动框架
9. **Verilog实现**: 可综合的PCIe Endpoint硬件设计

理解PCIe对于系统架构师、驱动开发者和硬件工程师都至关重要。PCIe技术的持续演进(5.0, 6.0, CXL)将继续推动计算系统的性能边界。


---

## 深入理解

### 核心原理

深入探讨技术原理和实现细节。

### 实践应用

- 应用场景1
- 应用场景2
- 应用场景3

### 最佳实践

1. 理解基础概念
2. 掌握核心机制
3. 应用到实际项目

---

> **最后更新**: 2026-03-21  
> **维护者**: AI Code Review
