# 调用约定与 ABI

> **层级**: L3 (核心层)  
> **目标**: 掌握 C 语言调用约定

---

## 1. 调用约定类型

cdecl、stdcall、fastcall、thiscall、vectorcall。

---

## 2. 参数传递

寄存器传递、栈传递、对齐要求、可变参数。

---

## 3. 返回值处理

寄存器返回、栈返回、大对象返回、隐藏参数。

---

## 4. 跨平台兼容

System V AMD64 ABI、Windows x64、ARM AAPCS。

---

## 5. 总结

调用约定定义函数调用的二进制接口规范。
